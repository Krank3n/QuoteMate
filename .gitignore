# Dependencies
/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

# Expo
.expo/
dist/
dist-web/
dist-web-demo/
dist-prod-web/
web-build/

# Environment variables
.env
.env.*
.env.local

# IDE
.vscode/*
.idea/

# Firebase
.firebase/

# Audit script output (contains sanitized customer quote data)
audit-recent-quotes.json
audit-*.json
quote-replay-priced-audit*.json

# OS
.DS_Store
*.swp
*.swo
*~

# Native directories (managed by expo prebuild)
/ios/
/android/

# Build
build/
builds/
/tmp
/out-tsc
*.log

# Android Keystore & Signing
*.keystore
*.jks
keystore.properties
android/keystore.properties
android/app/upload.keystore
android/app/release.keystore
android/app/release-new.keystore
@krank3n__quotemate.jks

# Android Build Outputs
*.aab
*.apk
android/app/build/outputs/

# iOS Provisioning Profiles & App Store Connect API keys
*.mobileprovision
*.p8
AuthKey_*.p8

# Signing / submit credentials (App Store Connect .p8, Google Play service account JSON)
credentials/

# Claude Code
.claude/scheduled_tasks.lock
dist-staging

# Warragul ground-truth replay outputs (testWarragulPlan.ts)
functions/warragul-replay-*.json

# Bake-off harness caches and results (functions/scripts/bakeoff).
# Scrape/product caches are prod-derived, and results files embed REAL customer
# job scopes — this repo is public, so none of it may ever be committed.
.bakeoff-*.json
bakeoff-*.json
customer-jobs-corpus*.json

# Apple Tap to Pay submission videos — real seller account + customer data, never commit
recordings/
